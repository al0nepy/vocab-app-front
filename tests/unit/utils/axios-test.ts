import axios from 'front-vocab/utils/axios';
import { module, test } from 'qunit';

module('Unit | Utility | axios', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    const result = axios();
    assert.ok(result);
  });
});
